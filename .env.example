# Database Configuration
# For local development, you can use SQLite (uncomment line below)
# DATABASE_URL="file:./dev.db"

# For production (PostgreSQL on Vercel/Neon), use these variables:
# These will be automatically set by Vercel when you connect a database
POSTGRES_PRISMA_URL="postgresql://user:password@host:5432/database?pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host:5432/database"

# Optional: If using SQLite for local development
# Uncomment the line below and comment out PostgreSQL URLs
# DATABASE_URL="file:./dev.db"
